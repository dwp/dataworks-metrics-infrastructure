jobs:
  - name: cycle-cloudwatch-exporter-production
    plan:
      - .: (( inject meta.plan.cycle-containers ))
        params:
          AWS_ROLE_ARN: arn:aws:iam::((aws_account.production)):role/ci
          CLUSTER: main
          SERVICE: cloudwatch-exporter
