jobs:
  - name: cycle-hbase-exporter-production
    plan:
      - .: (( inject meta.plan.cycle-containers ))
        params:
          AWS_ROLE_ARN: arn:aws:iam::((aws_account.production)):role/ci
          CLUSTER: metrics
          SERVICE: hbase-exporter
