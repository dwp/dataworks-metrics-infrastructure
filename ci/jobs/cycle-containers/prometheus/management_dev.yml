jobs:
  - name: mgmt-dev-prometheus
    plan:
      - get: dataworks-metrics-infrastructure
        trigger: false
        passed:
          - integration-prometheus
      - .: (( inject meta.plan.cycle-containers ))
        params:
          AWS_ROLE_ARN: arn:aws:iam::((aws_account.management-dev)):role/ci
          CLUSTER: main
          SERVICE: prometheus
